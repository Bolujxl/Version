let age = 23
let score = 2123
let requiredAge = 18
let requiredScore = 3200


let name = `bolujxl`
let requiredName = `bolujxl`

if (name === requiredName) {
    console.log("Name matches")} 
else {
    console.log("Name does not match")
}

if (age >= requiredAge && score >= requiredScore) {
    console.log("You are eligible to use the program")
}
else{
    console.log("You are not eligible enough to use the program")
}